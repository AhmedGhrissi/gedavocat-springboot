apiVersion: 1

# Datasource Loki — logs centralisés (réseau Docker interne uniquement)
datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 5000
      timeout: 60
