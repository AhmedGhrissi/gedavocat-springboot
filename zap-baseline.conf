# ZAP Baseline Scan Configuration for DocAvocat
# WARN = alerte non-bloquante, IGNORE = desactivee, FAIL = bloque le scan

# Cookie without SameSite — gere par Spring Security (SameSite=Strict)
10054	WARN	(Cookie without SameSite Attribute)

# CSP — gere dynamiquement par CspNonceFilter
10038	WARN	(Content Security Policy (CSP) Header Not Set)

# Application error disclosure — pages d'erreur Spring Boot customisees
90022	WARN	(Application Error Disclosure)

# X-Content-Type-Options — deja gere par Spring Security
10021	WARN	(X-Content-Type-Options Header Missing)

# Storable/Cacheable responses — normal pour pages publiques
10049	IGNORE	(Storable and Cacheable Content)
10050	IGNORE	(Retrieved from Cache)
